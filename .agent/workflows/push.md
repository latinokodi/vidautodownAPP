---
description: how to commit and push all changes
---

1. Run `git add .`
2. Run `git commit -m "[commit message]"`
3. Run `git push`

// turbo-all
Usage:
```bash
git add .
git commit -m "message"
git push
```
